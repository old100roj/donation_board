import state from './pagination/state'
import getters from './pagination/getters'
import mutations from './pagination/mutations'
import actions from './pagination/actions'

export default {
  state: state,
  getters: getters,
  mutations: mutations,
  actions: actions
}
